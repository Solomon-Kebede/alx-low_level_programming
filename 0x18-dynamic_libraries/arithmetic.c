/**
 * add - adds x and y
 * @x: integer
 * @y: integer
 * Return: sum
 */

int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}

/**
 * sub - subtracts x from y
 * @x: integer
 * @y: integer
 * Return: difference
 */

int sub(int x, int y)
{
	int diff;

	diff = x - y;
	return (diff);
}

/**
 * mul - multiply x and y
 * @x: integer
 * @y: integer
 * Return: product
 */

int mul(int x, int y)
{
	int product;

	product = x * y;
	return (product);
}
/**
 * div - divide x by y
 * @x: integer
 * @y: integer
 * Return: quotient
 */

int div(int x, int y)
{
	int quotient;

	quotient = x / y;
	return (quotient);
}
/**
 * mod - mods x by y
 * @x: integer
 * @y: integer
 * Return: modulus
 */

int mod(int x, int y)
{
	int modulus;

	modulus = x % y;
	return (modulus);
}
