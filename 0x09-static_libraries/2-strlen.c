#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen -return string length
 * @s: pointer to character
 * Return: length of string
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
