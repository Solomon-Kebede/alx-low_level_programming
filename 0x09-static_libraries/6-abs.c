#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @n: some user fed number
 * Return: n if +ve. -n if -ve
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);

}
